// Dynamic or loosely typed languages

// Numbers
var x = 10;

// Strings
x = "Piyush Garg";

// Booleans
var isActive = true;

// null
var value = null;

// undefined
var value2 = undefined;

// array
var arr = [1, 2, 3, 4, "Strings", true, false, "🥳", null];

// Objects
var obj = { name: "Piyush", surname: "Garg", isActive: true };

console.log("Value of x is", x);

function add(x, y) {
  return x + y;
}

console.log("Sum is", add(2, 3));
console.log("Sum is", add(2, 10));

// Variables
// (var, let) & const

// Made with var and let are mut
let variableName = 10;

variableName = 20;

variableName = 30;

variableName = "Piyush Garg";

// Variable made with const are im-mut
const age = 50;

// TDZ
